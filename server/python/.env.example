# Server Configuration
HOST=0.0.0.0
PORT=8080
ENVIRONMENT=development

# Authentication
JWT_SECRET=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Database (optional - leave empty for memory-only mode)
DATABASE_URL=
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# Redis (optional - leave empty for single-instance mode)
REDIS_URL=
REDIS_CHANNEL_PREFIX=synckit

# CORS
CORS_ORIGINS=["*"]
